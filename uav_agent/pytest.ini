[pytest]
markers =
    yolo_smoke: opt-in smoke test that loads a real local Ultralytics checkpoint
    qwen_lora_integration: opt-in test that loads the local Qwen3-VL model and runs a real LoRA optimizer step
